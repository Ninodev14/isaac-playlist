<%= form_with(model: playlist) do |form| %>
  <% if playlist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(playlist.errors.count, "error") %> prohibited this playlist from being saved:</h2>

      <ul>
        <% playlist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="raw">
<div class="col-3"></div>
  <div>
    <%= form.label :song, style: "display: block" %>
    <input class="form-control form-control-lg" type="text" placeholder="ecrire nom" aria-label="<%= form.text_area :song %>">
  </div>

  <div>
  
    <%= form.label :name, style: "display: block" %>
    <input class="form-control form-control-lg" type="text" placeholder="ecrire nom" aria-label="<%= form.text_field :name %>">
    
  </div>

  <div>
<%= form.label :name, style: "display: block" %>
    <input class="form-control form-control-lg" type="text" placeholder="ecrire nom" aria-label="<%= form.text_area :image %>">
  </div>
<div>
  <div>    
  <button class="btn btn-primary mb-3" style="margin-top: 2%;" type="<%= form.submit %>Creat Playlist</button>
  </div>
<% end %>
</div>
