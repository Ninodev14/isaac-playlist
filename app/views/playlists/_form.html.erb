

<%= form_with(model: playlist) do |form| %>
  <% if playlist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(playlist.errors.count, "error") %> prohibited this playlist from being saved:</h2>

      <ul>
        <% playlist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container-fluid">



  <div class="form">
  
    <%= form.label :name, style: "display: block" %>
    <input class="form-control form-control-lg" type="text" placeholder="Ecrire la demande..." aria-label="<%= form.text_field :name %>
  </div>

  <div class="form">
    <%= form.label :song, style: "display: block" %>
    <input class="form-control form-control-lg" type="text" placeholder="Donner lien du son..." aria-label="<%= form.text_field :song %>
  </div>

  <div class="form">
<%= form.label :image, style: "display: block" %>
    <input class="form-control form-control-lg" type="text"  placeholder="Url de l'image..." aria-label="<%= form.text_field :image %>
  </div>

<button class="btn btn-success mb-3" style="background-color:rgb(30, 30, 40); margin-top: 5vh" type="<%= form.submit %>Done</button>
<div>
<% end %>
</div>

